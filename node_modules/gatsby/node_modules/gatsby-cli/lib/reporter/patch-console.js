"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

exports.__esModule = true;
exports.patchConsole = void 0;

var _util = _interopRequireDefault(require("util"));

/*
 * This module is used to patch console through our reporter so we can track
 * these logs
 */
const patchConsole = reporter => {
  console.log = (format, ...args) => {
    if (format) {
      reporter.log(_util.default.format(format, ...args));
      return;
    }

    reporter.log();
  };

  console.warn = (format, ...args) => {
    if (format) {
      reporter.warn(_util.default.format(format, ...args));
      return;
    }

    reporter.warn();
  };

  console.info = (format, ...args) => {
    if (format) {
      reporter.info(_util.default.format(format, ...args));
      return;
    }

    reporter.info();
  };

  console.error = (format, ...args) => {
    reporter.error(_util.default.format(format, ...args));
  };
};

exports.patchConsole = patchConsole;